const user_info={
    age:18,
    firstName:"yehuda",
    lastName:"sanay",
}
function printUserInfo(user_info){
    user_info.forEach(element => {
        if(user_info.age>1){
        h1_id.innerHTML=element[1];}
    });

}
printUserInfo()
